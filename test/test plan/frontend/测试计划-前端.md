# 小说管理App — 前端测试计划 v1.1

> **测试版本**：v1.1 | **更新日期**：2026-08-17 | **测试地址**：http://localhost:5174

---

## 1. 测试概览

### 1.1 测试目标

基于项目 v1.1 版本的前端迭代，全面覆盖 7 个页面 / 8 个路由的功能、交互和兼容性：

- 路由守卫（8 条）
- 登录页（6 条）
- 注册页（6 条）
- 书架页（19 条）— 含通用抓取提示 + v1.3.2 按钮触发搜索
- 搜索页（6 条）
- 详情页（11 条）
- 设置页（5 条）
- **自定义源站管理页（12 条）— v1.1 新增**

### 1.2 被测系统

| 组件 | 技术栈 | 地址 |
|------|--------|------|
| 前端 SPA | Vue 3 + TypeScript + Vite + Pinia + Element Plus | http://localhost:5174 |
| 后端 API | FastAPI | http://localhost:8000 |

### 1.3 版本变更（v1.0 → v1.1）

| 新增 / 变更 | 说明 |
|-------------|------|
| `/crawl-sources` 路由 | 自定义源站管理页面 |
| `CrawlSourcesView.vue` | 新增：源站列表、添加/编辑弹窗、规则测试、帮助文档 |
| `ShelfView.vue` | 更新：添加书籍对话框文案提示"支持任意小说网站 URL" |
| `router/index.ts` | 新增 `/crawl-sources` 路由 |
| `types/index.ts` | 新增 `CrawlSource`、`CrawlSourceFormData`、`CrawlSourceTestRequest`、`CrawlSourceTestResult` 类型 |
| `api/books.ts` | 新增 6 个自定义源站 API 函数 |

---

## 2. 测试用例

### 2.1 路由守卫（RT）— 8 个用例

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-RT-01 | 未登录访问 `/shelf` | 跳转 `/login` |
| FE-RT-02 | 未登录访问 `/search` | 跳转 `/login` |
| FE-RT-03 | 未登录访问 `/detail/xxx` | 跳转 `/login` |
| FE-RT-04 | 未登录访问 `/settings`（v1.6 已下线） | 404 / 跳转 `/shelf` |
| FE-RT-05 | 未登录访问 `/crawl-sources` | 跳转 `/login` |
| FE-RT-06 | 已登录访问 `/login` | 跳转 `/shelf` |
| FE-RT-07 | 已登录访问 `/register` | 跳转 `/shelf` |
| FE-RT-08 | 根路径 `/` | 重定向 `/shelf` |

### 2.2 登录页（LOGIN）— 6 个用例

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-LOGIN-01 | 输入有效邮箱密码并提交 | 跳转 `/shelf` |
| FE-LOGIN-02 | 空邮箱/密码提交 | 表单校验阻止，显示"请输入邮箱" |
| FE-LOGIN-03 | 错误格式邮箱 | 表单校验阻止，显示"邮箱格式不正确" |
| FE-LOGIN-04 | 错误凭据提交 | 显示服务端错误消息（"邮箱或密码错误"） |
| FE-LOGIN-05 | 点击"立即注册"链接 | 跳转 `/register` |
| FE-LOGIN-06 | 登录中按钮状态 | 显示 loading 状态，按钮禁用 |

### 2.3 注册页（REG）— 6 个用例

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-REG-01 | 合法数据注册 | 跳转 `/shelf`（自动登录） |
| FE-REG-02 | 密码不一致 | 校验阻止 |
| FE-REG-03 | 密码纯数字 | 校验阻止 |
| FE-REG-04 | 密码纯字母 | 校验阻止 |
| FE-REG-05 | 密码 < 8 位 | 校验阻止 |
| FE-REG-06 | 重复邮箱 | 显示服务端错误 |

### 2.4 书架页（SHELF）— 19 个用例

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-SHELF-01 | 加载书架 | 书籍卡片列表 + 顶部"共 N 本小说" |
| FE-SHELF-02 | 空书架 | "书架空空如也"占位 + "去搜索"按钮 |
| FE-SHELF-03 | 点击"添加小说" | 弹窗打开 |
| FE-SHELF-04 | 填写书名/作者/源站 URL 并提交 | toast "已添加到书架" |
| FE-SHELF-05 | 添加书籍时书名留空 | 校验阻止 |
| FE-SHELF-06 | 输入源站 URL 后点击"检测" | 显示连通性检测结果（可达/不可达） |
| FE-SHELF-07 | 检测不可达 URL | 显示错误信息和修复建议 |
| FE-SHELF-08 | **源站链接输入框下方提示文案** | 显示"支持任意小说网站 URL"及"管理自定义源站规则"链接 |
| FE-SHELF-09 | 点击"管理自定义源站规则"链接 | 跳转 `/crawl-sources` |
| FE-SHELF-10 | 删除书籍 | 二次确认弹窗 → 确认 → toast "小说已删除" |
| FE-SHELF-11 | 删除确认弹窗中取消 | 不删除 |
| FE-SHELF-12 | 点击书封 / 书名 | 跳转 `/detail/:id` |
| FE-SHELF-13 | 分页切换 | 加载对应页数据 |
| FE-SHELF-14 ⭐ v1.3.2 新增 | 输入关键词后**点击「搜索」按钮** | 列表过滤为匹配书籍，显示"搜索 \"xx\" 找到 N 本" |
| FE-SHELF-15 ⭐ v1.3.2 新增 | 输入关键词后**按 Enter** | 与点击搜索按钮效果一致 |
| FE-SHELF-16 ⭐ v1.3.2 新增 | 关键词 < 2 字符 | 「搜索」按钮禁用 |
| FE-SHELF-17 ⭐ v1.3.2 新增 | 搜索生效后再编辑关键词（不点搜索） | 旧结果失效、列表恢复书架视图，显示提示 |
| FE-SHELF-18 ⭐ v1.3.2 新增 | AI 语义模式输入描述后**点击「AI 搜索」** | 显示 AI 搜索结果（含匹配分数和原因），按钮带 loading |
| FE-SHELF-19 ⭐ v1.3.2 新增 | AI 搜索中编辑关键词 | AI 请求作废、旧结果清除，需重新点击搜索 |

### 2.5 搜索页（SEARCH）— 6 个用例

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-SEARCH-01 | 初始加载 | 搜索引导文案 + 空输入框 |
| FE-SEARCH-02 | 从 URL 参数 `?q=xxx` 自动搜索 | 自动触发搜索并展示结果 |
| FE-SEARCH-03 | 输入关键词按 Enter 搜索 | 卡片列表展示 |
| FE-SEARCH-04 | 无结果 | "未找到相关小说，请尝试其他关键词" |
| FE-SEARCH-05 | 点击"查看详情" | 跳转 `/detail/:id` |
| FE-SEARCH-06 | 点击"← 返回书架" | 跳转 `/shelf` |

### 2.6 详情页（DETAIL）— 11 个用例

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-DETAIL-01 | 加载详情 | 书名、作者、状态、添加时间 |
| FE-DETAIL-02 | 有 `source_url` | 显示来源链接 |
| FE-DETAIL-03 | 无 `source_url` | 不显示来源链接 |
| FE-DETAIL-04 | 点击"抓取" | 按钮 loading → 状态更新 |
| FE-DETAIL-05 | `status:crawling` 时 | 显示进度条 / 按钮禁用 |
| FE-DETAIL-06 | `status:done` 时 | 显示"下载 EPUB"和"下载 TXT"按钮 |
| FE-DETAIL-07 | 点击"下载 EPUB" | 触发 EPUB 文件下载 |
| FE-DETAIL-08 | 点击"下载 TXT" | 触发 TXT 文件下载 |
| FE-DETAIL-09 | `status:failed` 时 | 显示失败信息和错误原因 |
| FE-DETAIL-10 | 查看不存在的书籍 | 显示"该书籍不存在或已被删除" |
| FE-DETAIL-11 | 点击"← 返回书架" | 跳转 `/shelf` |

### 2.7 设置页（SETTINGS）— 5 个用例 ⚠️ v1.6 已下线

> **v1.6 变更**：原 `/settings` 独立设置页已下线，修改密码与退出登录合并进顶栏用户头像下拉菜单（改为弹窗交互）。以下用例由 2.9 顶栏头像下拉用例取代，仅保留作为历史记录。

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| ~~FE-SET-01~~ | ~~加载页面~~ | ~~显示当前登录邮箱~~ |
| ~~FE-SET-02~~ | ~~修改密码成功~~ | ~~toast 成功消息 + 清空表单~~ |
| ~~FE-SET-03~~ | ~~旧密码错误~~ | ~~显示服务端错误~~ |
| ~~FE-SET-04~~ | ~~退出登录~~ | ~~确认弹窗 → 确认 → 跳转 `/login`~~ |
| ~~FE-SET-05~~ | ~~退出取消~~ | ~~停留在设置页~~ |

### 2.8 顶栏头像下拉菜单（USER）— 6 个用例 ⭐ v1.6 新增

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-USER-01 | 点击顶栏用户头像 | 弹出下拉菜单，显示"修改个人密码"与"退出登录"两项 |
| FE-USER-02 | 选择"修改个人密码" | 弹出修改密码弹窗（含当前/新/确认三个输入框） |
| FE-USER-03 | 修改密码成功 | toast 成功消息 + 弹窗关闭 |
| FE-USER-04 | 旧密码错误 | 弹窗内显示服务端错误 |
| FE-USER-05 | 选择"退出登录" | 确认弹窗 → 确认 → 跳转 `/login` |
| FE-USER-06 | 退出取消 | 保持当前页面与登录状态 |

### 2.9 自定义源站管理页（CS）— 12 个用例 ⭐ v1.1 新增

| 用例 ID | 场景 | 预期结果 |
|---------|------|---------|
| FE-CS-01 | 加载页面 | 源站规则列表 + 顶部"自定义抓取源站"标题 |
| FE-CS-02 | 空列表 | 显示"暂无自定义源站规则"及提示文案 |
| FE-CS-03 | 点击"添加源站" | 弹窗打开，含名称、URL、规则 JSON 三个输入框 |
| FE-CS-04 | 填写表单并提交 | toast "源站规则已添加"，列表刷新 |
| FE-CS-05 | 创建时 name 为空 | 校验阻止，显示"请输入源站名称" |
| FE-CS-06 | 创建时 url 为空 | 校验阻止，显示"请输入源站 URL" |
| FE-CS-07 | 创建时 rule_json 为空 | 校验阻止 |
| FE-CS-08 | 点击"编辑" | 弹窗打开，表单预填已有数据 |
| FE-CS-09 | 修改后提交 | toast "源站规则已更新"，列表刷新 |
| FE-CS-10 | 点击"删除" | 二次确认 → 确认 → toast "源站已删除" |
| FE-CS-11 | 点击"测试规则"按钮 | 显示测试结果（成功：章节数和示例 / 失败：错误描述） |
| FE-CS-12 | 底部帮助说明 | 显示规则配置字段说明表格（toc.item / chapter.title 等） |

---

## 3. 页面覆盖统计

| 页面 | 路由 | 用例数 | v1.1 新增 |
|------|------|--------|----------|
| 路由守卫 | — | 8 | +1 (`/crawl-sources`) |
| 登录页 | `/login` | 6 | 0 |
| 注册页 | `/register` | 6 | 0 |
| 书架页 | `/shelf` | 19 | +3 (检测按钮、提示文案、链接跳转) +6 (v1.3.2 按钮触发搜索) |
| 搜索页 | `/search` | 6 | 0 |
| 详情页 | `/detail/:id` | 11 | +2 (TXT 下载、失败状态) |
| ~~设置页~~ | ~~`/settings`~~ | ~~5~~（v1.6 下线） | 0 |
| 顶栏头像下拉 | 全局顶栏 | 6 ⭐ v1.6 新增 | 全部新增 |
| **自定义源站** | **`/crawl-sources`** | **12** | **全部新增** |
| **总计** | **8** | **79** | **24** |

---

## 4. 测试执行计划

| 阶段 | 内容 | 优先级 | 方式 |
|------|------|--------|------|
| Phase 1 | 路由守卫 + 登录/注册页面 | P0 | 手动浏览器测试 |
| Phase 2 | 书架 + 搜索 + 详情页面 | P0 | 手动 + API 辅助 |
| Phase 3 | 顶栏头像下拉菜单（修改密码 / 退出登录） | P0 | 手动 |
| Phase 4 | **自定义源站管理页面** | P1 | 手动 + API 验证 |
| Phase 5 | 响应式布局（375px / 768px / 1280px） | P2 | 浏览器 DevTools |

---

## 5. 测试环境

| 项目 | 详情 |
|------|------|
| 前端 | Vue 3 + Vite @ localhost:5174 |
| 后端 | FastAPI v1.1 @ localhost:8000 |
| 浏览器 | Chrome（主要）/ Firefox |
| 测试账号 | `testuser@test.com` / `test123456` |

---

> **文档版本**：v1.1 | **最后更新**：2026-08-17
> **v1.1 变更**：新增 `/crawl-sources` 路由守卫、自定义源站管理页 12 条用例、书架页源站 URL 提示文案验证、详情页 TXT 下载/Failed 状态验证、页面/路由从 7/7 扩至 8/8
> **v1.3.2 变更**：书架页新增 6 条按钮触发搜索用例（FE-SHELF-14 ～ 19），覆盖「搜索」按钮/Enter 触发、按钮禁用、编辑失效、AI 搜索按钮触发与请求作废；用例总数 73 → 79
