/* ═══════════════════════════════════════════════════════
   小说管理App · 全局常量
   ═══════════════════════════════════════════════════════ */

/**
 * 移动端（Capacitor）后端服务器地址。
 *
 * 移动端无法内嵌 Python 后端，必须先把 FastAPI 后端部署到服务器
 * （推荐 HTTPS + 域名），打包 APK 前把此常量改为实际地址。
 * 格式示例：'https://api.example.com'（结尾不带斜杠）
 */
export const MOBILE_API_BASE = 'http://47.120.32.118'
